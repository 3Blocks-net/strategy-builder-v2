import '@testing-library/jest-dom/vitest';
// Every rendered component may translate or format — the i18n instance has to
// exist before the first render, exactly as it does in `main.tsx`.
import '@/i18n';
