---
id: TASK-8
title: pnpm dev startet den Hardhat-Fork nicht (DX-Lücke)
status: In Progress
assignee:
  - auto-builder
created_date: '2026-07-26 10:10'
updated_date: '2026-07-26 10:35'
labels:
  - enhancement
  - ready-for-agent
dependencies: []
ordinal: 8000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
> *This was generated by AI during triage.*

**Expected:** `pnpm dev` startet den vollen lokalen Stack — oder sagt beim Start klar, was fehlt.
**Found:** `scripts/dev.mjs` startet DB, Migrationen, shared-watch, Backend, Frontend — aber keinen Hardhat-Fork (Port 8545) und keinen Hinweis darauf. Backend-`.env` zeigt mit RPC_URL=http://localhost:8545 ins Leere; der Indexer loggt dann pro Tick "failed to detect network".
**Warum wichtig:** Jeder Walkthrough/Onboarding stolpert hier; die Fehlermeldung führt in die falsche Richtung (sieht nach Backend-Bug aus).

## Agent Brief

**Category:** enhancement
**Summary:** dev.mjs um Fork-Handling ergänzen: erkennen, ob 8545 antwortet; sonst klarer Hinweis (oder optionaler Mitstart).

**Current behavior:** dev.mjs prüft Docker/Postgres, startet aber nie den Fork und warnt nicht, wenn RPC_URL ins Leere zeigt.

**Desired behavior:** Beim Start prüft dev.mjs per eth_chainId-Request, ob unter RPC_URL (aus packages/backend/.env, Default http://localhost:8545) ein Node antwortet. Wenn nein: gut sichtbarer Hinweis mit den drei Kommandos (contracts:fork:bsc, contracts:deploy:fork, db:seed) — Verhalten sonst unverändert. Optional (Kann): Flag `--fork`, das den Fork als vierten Kind-Prozess mitstartet.

**Acceptance criteria:**
- [ ] Ohne laufenden Fork zeigt `pnpm dev` den Hinweis mit den drei Kommandos
- [ ] Mit laufendem Fork erscheint kein Hinweis, Verhalten unverändert
- [ ] Kein Blockieren: dev.mjs startet Backend/Frontend in beiden Fällen weiter

**Out of scope:** Automatischer deploy-fork/seed; Änderungen an contracts-Skripten.

## Automatisierte Prüfung
- T: `node scripts/dev.mjs --check-fork-only` (oder äquivalenter testbarer Einstieg) → Exit 0 mit Hinweis-Text, wenn 8545 nicht antwortet

## Kontext-Kapsel
Rolle: Entwickler. Quelle: Nutzer-Walkthrough 2026-07-26 (Station-5-Abnahme).
<!-- SECTION:DESCRIPTION:END -->
