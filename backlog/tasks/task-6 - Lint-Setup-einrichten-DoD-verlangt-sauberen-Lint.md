---
id: TASK-6
title: Lint-Setup einrichten (DoD verlangt sauberen Lint)
status: Ready for Agent
assignee: []
created_date: '2026-07-25 11:44'
updated_date: '2026-07-25 11:53'
labels:
  - enhancement
  - ready-for-agent
dependencies: []
ordinal: 6000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
> *This was generated by AI during triage.*

**Expected:** Ein repo-weites Lint-Kommando als Backpressure für den Bau-Loop — die Definition of Done in CLAUDE.md verlangt "Lint sauber".
**Found:** Kein ESLint/Biome-Setup und kein lint-Script in Root oder Paketen; der Steckbrief führt Lint als TBD — needs research.
**Warum wichtig:** Ohne Lint-Kommando kann keine Station "Lint grün" prüfen — die DoD ist aktuell nicht maschinell erfüllbar.

## Akzeptanzkriterien
- [ ] Linter gewählt (z. B. ESLint flat config oder Biome) und für alle TS-Pakete konfiguriert
- [ ] Root-Script `pnpm lint` läuft über alle Pakete
- [ ] Steckbrief (docs/agents/projekt-steckbrief.md) mit dem Lint-Kommando aktualisiert

## Automatisierte Prüfung
- T: `pnpm lint` → Exit 0

## Kontext-Kapsel
Rolle: Entwickler. Quelle: brownfield-audit Steckbrief-Erhebung.
<!-- SECTION:DESCRIPTION:END -->

## Agent Brief

> *This was generated by AI during triage.*

**Category:** enhancement
**Summary:** Biome als Linter fürs Monorepo einrichten; `pnpm lint` als Root-Kommando.

**Current behavior:**
Kein Linter im Repo (kein ESLint/Biome, keine lint-Scripts). Die Definition of Done in CLAUDE.md verlangt „Lint sauber", ist aber nicht maschinell prüfbar; der Projekt-Steckbrief führt Lint als `TBD — needs research`.

**Desired behavior:**
Biome ist als Dev-Dependency im Root installiert, mit einer `biome.json`, die alle fünf TypeScript-Pakete abdeckt (Solidity bleibt außen vor). Linting läuft repo-weit über ein Root-Script `pnpm lint` (Biome `check`, ohne Auto-Fix im CI-Modus). Generierter Code und Build-Output sind ausgeschlossen (`**/generated/**`, `**/dist/**`, `**/build/**`, `node_modules`, Typechain-Artefakte, `backlog/`). Bestehende Verstöße sind behoben oder die betreffende Regel ist mit kurzer Begründung in der Config deaktiviert — `pnpm lint` läuft am Ende mit Exit 0. Der Projekt-Steckbrief (`docs/agents/projekt-steckbrief.md`) trägt das Lint-Kommando statt TBD.

**Key interfaces:**
- Root-`package.json`: Script `lint`
- `biome.json` im Root (eine Config fürs Monorepo; Formatter-Teil darf konfiguriert, muss aber nicht erzwungen werden)
- `docs/agents/projekt-steckbrief.md`, Zeile „Lint"

**Acceptance criteria:**
- [ ] `pnpm lint` → Exit 0
- [ ] Alle fünf Pakete werden geprüft (Stichprobe: absichtlicher Fehler in einem Paket schlägt an)
- [ ] Generierter/Build-Output ist ausgeschlossen (kein Finding aus dist/generated)
- [ ] Steckbrief aktualisiert

**Out of scope:**
- Repo-weiter Format-Rewrite (kein „biome format --write" über den ganzen Bestand in diesem Ticket)
- ESLint-Kompatibilitäts-Migrationen, CI-Workflow-Anlage
- Solidity-Linting (solhint o. ä.)
