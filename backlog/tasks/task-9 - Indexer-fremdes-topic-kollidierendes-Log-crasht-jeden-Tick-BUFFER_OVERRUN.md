---
id: TASK-9
title: 'Indexer: fremdes topic-kollidierendes Log crasht jeden Tick (BUFFER_OVERRUN)'
status: In Progress
assignee:
  - auto-builder
created_date: '2026-07-26 10:10'
updated_date: '2026-07-26 10:16'
labels:
  - bug
  - ready-for-agent
dependencies: []
ordinal: 9000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
> *This was generated by AI during triage.*

**Expected:** Der Indexer überspringt Logs fremder Contracts und kommt mit jedem wohlgeformten Chain-Zustand voran; ein einzelnes nicht-dekodierbares Log darf nie den Tick beenden.
**Found:** `tick()` holt Logs adressenlos (nur Topic-Filter), dekodiert via `parseVaultLog` und filtert ERST DANACH auf bekannte Vaults (packages/backend/src/indexer/indexer.service.ts:200-204). Ein fremder Contract mit gleicher Event-Signatur, aber anderer indexed-Verteilung (Log-Data 32 Bytes, unser Withdrawn-Decode liest bis Offset 64) wirft `data out-of-bounds (BUFFER_OVERRUN)` — ungefangen → "Indexer tick failed". Der Cursor advanced nie über die Range → Endlos-Retry, Indexing steht komplett.
**Warum wichtig:** Auf BSC-Mainnet(-Fork) kann JEDER beliebige Contract dieses Muster emittieren — ein einziges fremdes Log legt das gesamte Indexing dauerhaft still (DoS-Charakter). Live reproduziert im Walkthrough am 2026-07-26.

## Agent Brief

**Category:** bug
**Summary:** Vault-Gate vor das Dekodieren ziehen UND parseVaultLog decode-fehlertolerant machen; Regressionstest mit malformtem Kollisions-Log.

**Current behavior:** logs.map(parseVaultLog) wirft bei topic-kollidierendem Fremd-Log; Vault-Filter kommt zu spät; Tick bricht ab, Cursor bleibt stehen.

**Desired behavior:** (1) Logs werden VOR dem Parsen auf bekannte Vault-Adressen gefiltert (getAddress(log.address) in vaultByAddress). (2) Zusätzlich fängt parseVaultLog (packages/backend/src/indexer/event-mapper.ts, parseLog-Aufruf) Decode-Fehler und gibt null zurück (Debug-Log mit txHash/logIndex) — Gürtel+Hosenträger, weil auch ein bekannter Vault per delegatecall kollidierende Events emittieren könnte. (3) Ein Tick mit solchen Logs läuft durch und advanced den Cursor.

**Key interfaces:**
- `parseVaultLog(log): ParsedVaultLog | null` — Contract erweitert um: null auch bei Decode-Fehler (nicht nur bei unbekanntem Topic)
- `IndexerService.tick()` — Reihenfolge: address-gate → parse
- Kein Schema-/API-Change

**Acceptance criteria:**
- [x] Neuer Test (event-mapper.spec): Log mit Withdrawn-topicHash, 4 Topics, 32-Byte-Data → parseVaultLog === null, kein Throw
- [x] Neuer Test (indexer.integration.spec): Range mit fremdem Kollisions-Log + echtem Vault-Event → Vault-Event persistiert, Cursor advanced
- [x] Bestehende Indexer-Tests grün

**Out of scope:** Umstellung auf adress-gebundene getLogs-Filter pro Vault (Performance-Thema, eigenes Ticket falls gewünscht); Protokoll-Flow-Pfad (bereits warn-gefangen).

## Automatisierte Prüfung
- T: `pnpm --filter backend exec jest src/indexer` → Exit 0

## Kontext-Kapsel
Rolle: Entwickler. Quelle: Nutzer-Report 2026-07-26, Fehlerbild: ERROR [IndexerService] Indexer tick failed: data out-of-bounds (buffer=0x…077e772392b60000, length=32, offset=64, BUFFER_OVERRUN, ethers 6.16.0).
<!-- SECTION:DESCRIPTION:END -->
