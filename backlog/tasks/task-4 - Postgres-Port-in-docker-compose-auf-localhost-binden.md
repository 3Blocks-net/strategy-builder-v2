---
id: TASK-4
title: Postgres-Port in docker-compose auf localhost binden
status: Done
assignee:
  - auto-builder
created_date: '2026-07-25 11:44'
updated_date: '2026-07-25 12:07'
labels:
  - enhancement
  - ready-for-agent
dependencies: []
ordinal: 4000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
> *This was generated by AI during triage.*

**Expected:** Deny-by-default: die Dev-Datenbank ist nur lokal erreichbar.
**Found:** docker-compose.yml bindet "5432:5432" — Postgres lauscht auf allen Interfaces des Hosts (Standard-Credentials pecunity/pecunity aus .env.example).
**Warum wichtig:** Im Home-/Office-Netz oder Hotspot ist die Dev-DB von außen erreichbar; mit bekannten Default-Credentials ein unnötiges Einfallstor.

## Akzeptanzkriterien
- [x] Port-Mapping auf "127.0.0.1:5432:5432" geändert
- [ ] `pnpm db:up` + Backend-Start funktionieren unverändert

## Automatisierte Prüfung
- T: `grep -q '127.0.0.1:5432:5432' docker-compose.yml` → Exit 0

## Kontext-Kapsel
Rolle: Entwickler. Quelle: brownfield-audit Security-Baseline (Deny-by-default-Zeile).
<!-- SECTION:DESCRIPTION:END -->

## Agent Brief

> *This was generated by AI during triage.*

**Category:** enhancement
**Summary:** Dev-Postgres nur noch auf localhost binden.

**Current behavior:**
Der `db`-Service in `docker-compose.yml` mappt `"5432:5432"` — Postgres ist auf allen Host-Interfaces erreichbar, mit den Dev-Credentials aus `.env.example`.

**Desired behavior:**
Das Port-Mapping bindet ausschließlich an Loopback (`"127.0.0.1:5432:5432"`). `pnpm db:up`, Prisma-Migration/Seed und der Backend-Start (DATABASE_URL zeigt auf localhost) funktionieren unverändert.

**Key interfaces:**
- `docker-compose.yml`, Service `db`, Feld `ports`
- `DATABASE_URL` in `packages/backend/.env.example` (localhost — bleibt kompatibel, nicht ändern)

**Acceptance criteria:**
- [x] `grep -q '127.0.0.1:5432:5432' docker-compose.yml` → Exit 0
- [ ] `pnpm db:up` startet den Container fehlerfrei
- [ ] Backend-Tests (`pnpm backend:test`) laufen grün gegen die DB

**Out of scope:**
- Credentials/Datenbankname ändern
- Weitere Services im Compose-File anfassen
